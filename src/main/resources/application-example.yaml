spring:
  application:
    name: finsight


  # Database config

  datasource:
    url: <localhost or cloud url>
    username: <username if any>
    password: <password>

  jpa:
    hibernate:
      ddl-auto: update
  show-sql: true
  properties:
    hibernate:
      format_sql: true
